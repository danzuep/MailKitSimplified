name: Release
run-name: Publish 'v#.#.#' tags as releases

on:
  workflow_dispatch:
  push:
    branches:
      - release/**
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  pipeline:
    name: CI/CD
    uses: ./.github/workflows/_pipeline.yml
    with:
      environment: release
    secrets: inherit

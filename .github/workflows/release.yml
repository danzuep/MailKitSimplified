name: Release
run-name: Publish 'v1.2.3' tags as releases

on:
  workflow_dispatch:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
    branches:
      - release/**

jobs:
  pipeline:
    name: 🪜 CI/CD
    uses: ./.github/workflows/_pipeline.yml
    with:
      environment: release
    secrets: inherit
